name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Replace/setup these steps for the actual project's stack.
      # Example for Node.js:
      #
      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 22
      #     cache: npm
      #
      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Validate
      #   run: npm run validate

      - name: CI configuration reminder
        run: |
          echo "Configure the project-specific runtime, dependency installation, and validation command."
          echo "See docs/DEVELOPMENT.md."
